# DogBot Frontend (WhatsApp)

Estrutura inicial do serviço do bot (whatsapp-web.js). Conteúdo mínimo para desenvolvimento local.

Como usar:

1. Copie `.env.example` para `.env` e ajuste `BOT_SECRET`.
2. Instale dependências: `npm install`.
3. Inicie em desenvolvimento: `npm run dev`.
